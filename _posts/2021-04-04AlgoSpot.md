---
title: "알고스팟 : 분할정복"
categories:
  - Algorithm
tags:
  - 분할정복
  - JAVA
---
- 뒤집기 코드

    ```java
    public static String recursive(String input) {
     
            pointer += 1;
            char ch = input.charAt(pointer);
            if (ch == 'b' || ch == 'w')
                return ch + "";
     
            String leftUp = recursive(input);
            String rightUp = recursive(input);
            String leftDown = recursive(input);
            String rightDown = recursive(input);
     
            return "x" + leftDown + rightDown + leftUp + rightUp;
     
        }
    ```

    - pointer가 -1부터 시작하므로 첫 글자가 x가 아니면 그대로 return 하고 x이면 leftup, rightup,leftdown, rightdown을 찾는 연산을 시작한다. x이면

        ```java
         if (ch == 'b' || ch == 'w')
                    return ch + "";
        ```

        이부분을 skip하고 return이 없으므로 rightdown까지 다 구해서 뒤집기 전까지 return이 없음

    - x를 기준으로 분할 정복한다고 볼 수 있음